const config = {
  env: {
    local: {
      apiBase: "/"
    },
    dev: {
      apiBase: "/"
    },
    demo: {
      apiBase: "/"
    },
    production: {
      apiBase: "/"
    }
  }
};

module.exports = config;
